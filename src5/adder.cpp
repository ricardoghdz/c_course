#include <iostream>

using namespace std;
int main(void)
{
    int x,y;
    // prompt user for x
    cout<<"x is ";
    cin>>x;

    // prompt user for y
    cout<<"y is ";
    cin>>y;

    // calculate sum for user
    cout<<"sum is "<< x + y<<endl;
}